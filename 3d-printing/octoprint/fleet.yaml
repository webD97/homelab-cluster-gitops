defaultNamespace: 3d-printing

dependsOn:
  - selector:
      matchLabels:
        gateway-api-crds: "true"

ignore:
  conditions:
    # Ignored because pod cannot start if serial port not present
    - type: Available
      status: "False"
