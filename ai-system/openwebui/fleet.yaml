defaultNamespace: ai-system

helm:
  repo: https://helm.openwebui.com/
  chart: open-webui
  version: 5.4.0
  releaseName: openwebui
  values:
    ollama:
      persistentVolume:
        enabled: true
        size: 100Gi
    pipelines:
      enabled: false
